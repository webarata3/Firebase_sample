{
  "rules": {
    ".read": false,
    ".write": false,
    "chat": {
      ".read": true,
      ".write": true,
      "$messageId": {
        "name": {
          ".validate": "newData.val().length > 0"
        },
        "message": {
         ".validate": "newData.val().length > 0 && newData.val().length <= 30"
        }
      }
    }
  }
}
